
&НаКлиенте
Процедура КомментарийОткрытие(Элемент, СтандартнаяОбработка)
	ТекстКомментария = Комментарий.ПолучитьДанные;
	//Сообщить (ТекстКомментария);
	МассивПодстрок = СтрРазделить(ТекстКомментария, " ");
	Счетчик = 0;
	Для Каждого Элемент Из МассивПодстрок Цикл
		  Позиция = Найти(Элемент, "e1cib/");
		  Если Позиция > 0 Тогда
		  	НомерЭлементаССсылкой = Счетчик;
		  КонецЕсли;
		  Счетчик=Счетчик+1;
	КонецЦикла;
	НавигационнаяСсылка = МассивПодстрок[НомерЭлементаССсылкой];
	ПерейтиПоНавигационнойСсылке(НавигационнаяСсылка);
КонецПроцедуры


&НаКлиенте
Процедура ТоварыЦенаПриИзменении(Элемент)
	
	Для Каждого ТекСтрокаТовары Из Объект.Товары Цикл
		 ТекСтрокаТовары.Сумма = ТекСтрокаТовары.Количество * ТекСтрокаТовары.Цена;
	КонецЦикла;

КонецПроцедуры

&НаКлиенте
Процедура ТоварыКоличествоПриИзменении(Элемент)
	Для Каждого ТекСтрокаТовары Из Объект.Товары Цикл
		 ТекСтрокаТовары.Сумма = ТекСтрокаТовары.Количество * ТекСтрокаТовары.Цена;
	КонецЦикла;
КонецПроцедуры

